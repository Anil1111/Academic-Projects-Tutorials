import { PropertyBindingHighlightDirective } from './property-binding-highlight.directive';

describe('PropertyBindingHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new PropertyBindingHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
