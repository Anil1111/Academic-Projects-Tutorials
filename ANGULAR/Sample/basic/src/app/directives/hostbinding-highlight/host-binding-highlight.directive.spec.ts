import { HostBindingHighlightDirective } from './host-binding-highlight.directive';

describe('HostBindingHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HostBindingHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
